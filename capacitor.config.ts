import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: ' azure-swa-ionic-try',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
